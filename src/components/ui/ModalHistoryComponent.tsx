import { Box, Modal, Typography } from '@mui/material';
import React, { useContext } from 'react';
import UIContext from '../../context/ui/UIContext';

const style = {
  position: 'absolute' as const,
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  width: 400,
  bgcolor: 'background.paper',
  boxShadow: 24,
  p: 4,
};

function ModalHistoryComponent() {
  const { isModalHistoryOpen } = useContext(UIContext);
  return (
    <Modal open={isModalHistoryOpen} aria-labelledby="modal-modal-title" aria-describedby="modal-modal-description">
      <Box sx={style}>
        <Typography id="modal-modal-title" variant="h6" component="h2">
          Text in a modal
        </Typography>
        <Typography id="modal-modal-description" sx={{ mt: 2 }}>
          Duis mollis, est non commodo luctus, nisi erat porttitor ligula.
        </Typography>
      </Box>
    </Modal>
  );
}

export default ModalHistoryComponent;
